Antilatency Display Stylus Proxy - Windows x64
================================================

Start the proxy:

    run-proxy.cmd

The local endpoint is http://127.0.0.1:48192.
Stop other applications that own ADN before starting the proxy.
Keep every file from this directory together.
